version: 1.0
runtime: nodejs22

build:
  commands:
    build:
      - chmod +x build.sh start.sh
      - ./build.sh

run:
  runtime-version: 22.17.0
  command: ./start.sh
  network:
    port: 3000
    env: PORT
