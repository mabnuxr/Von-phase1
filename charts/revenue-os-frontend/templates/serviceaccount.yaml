apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "revenue-os-frontend.serviceAccountName" . }}
  labels:
    {{- include "revenue-os-frontend.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
