apiVersion: v1
kind: Service
metadata:
  name: {{ include "revenue-os-frontend.fullname" . }}
  labels:
    {{- include "revenue-os-frontend.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "revenue-os-frontend.selectorLabels" . | nindent 4 }}
